#include<bits/stdc++.h>
 
using namespace std;
 
void solve(){
    
    int n;
    cin >> n;
    
    if(n<=1399){
        cout<<"Division 4"<<"
";
    }
    else if(n<=1599){
        cout<<"Division 3"<<"
";
    }
    else if(n<=1899){
        cout<<"Division 2"<<"
";
    }
    else{
        cout<<"Division 1"<<"
";
    }
    
}
 
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    
    int n;
    cin >> n;
    while(n){
        solve();
        n--;
    }
    return 0;
}